-> 13/05/2026 19:39
Iniciando esse projeto com a ideia de criar meu primeiro sistema no 
Framework .NET, mais especificamente com C#.
Esse documento tem a finalizade de servir como um diário de bordo, onde vou
registrar minha linha de raciocínio e quais ideias tenho em mente.

A descrição completa do projeto está no arquivo `README.md`.

Sim, esse arquivo será bem informal, não espere nada técnico por aqui!

Logo de cara quero registrar que poderia sim fazer tudo com IA de uma forma
muito mais rápida, mas não pretendo utilizar disso por hora, tentarei fazer na
mão.

Esse é meu primeiro projeto com .NET, não tenho noção de por onde começar com a
linguagem, no entanto, quero registrar qual a minha ideia inicial.

BANCO DE DADOS

Penso que primeiro posso modelar o banco, acredito que existam ferramentas
online que auxiliem nisso, primeiro fazendo o diagrama para depois
criar/pegar o script para gerar o banco.

Como a regra de negócio está bem descrita, posso apenas aplicar isso ao banco,
que servirá como uma trava na hora de criar o sistema de fato.

DISPONIBILIDADE

Além disso, também não sei como deixar o projeto online, logo que começar a 
entender o framework já quero ver sobre isso, para que no final não seja uma
dor de cabeça.

-> 14/05/2026 10:05

Pretendo modelar o banco agora e deixar essa parte pronta, para depois focar
apenas no restante.

Iria utilizar o LucidChart, mas ele faz apenas diagramas, 
partindo para o dBeaver

Esquece, peguei um vídeo de como criar um sistema em .NET:
https://www.youtube.com/watch?v=6CitqpJEwBE

Estou instalando o .NET framework no meu Ubuntinho:
https://learn.microsoft.com/pt-br/dotnet/core/install/linux-ubuntu-install?tabs=dotnet10&pivots=os-linux-ubuntu-2604

https://www.alura.com.br/artigos/csharp-criando-solucao-csharp-linux-ubuntu

Instalei o dotnet com o comando:
sudo apt-get update && \
  sudo apt-get install -y dotnet-sdk-10.0

E executei um "dotnet new console" aqui no projeto, já criou alguns arquivos a 
mais.

Dei um "dotnet run" e mostrou "Hello, World"

Voltamos! Aparentemente não preciso de um console, mas sim de um webapi, 
por isso exclui tudo.

Rodei "dotnet new webapi", que teoricamente prepara o ambiente para servir
como uma API.

Para testar, "dotnet run"

Aparentemente essa versão não vem com o Swagger, que é uma forma de testar os 
endpoints, vou rodar "dotnet add package Swashbuckle.AspNetCore" para colocar
isso, além de alterar o "Program.cs" (helpzinho do chat).

Atualmente já consigo acessar "http://localhost:5193/weatherforecast" que me dá
um retorno de "exemplo".

Agora sim, consigo acessar o "http://localhost:5193/swagger" e possui uma inte-
rface bonitinha com todos os endpoints disponíveis.

Teoricamente agora preciso modelar o banco, fazer a conexão de alguma forma e 
criar os endpoints. Depois que estiver pronto passo para a interface, e por ul-
timo, para a publicação.

---

PRIMEIRO EDIT

Ok, seguindo a lógica de sintaxe da requisição anterior, fiz a requisição "tes-
te", que retorna um JSON simples. O importante é que funciona. É "só" utilizar
essa sintaxe para fazer as demais requisições conforme necessário, o famoso 
CRUD.

Documentei o Program.cs para me entender melhor.
Sei que o ideal seria criar tipos/classes para todos os objetos, tipo Pessoa
Compra, Item, CPF e etc, mas não sei se conseguirei logo de cara. Minha ideia é
fazer funcionar, depois melhora.

Comecei a modelar o banco pelo site https://dbdiagram.io/d
A principio tranquilo, só preciso revisar com calma os relacionamentos, em 
breve coloco aqui o resultado (ou em outro arquivo)

OK, modelagem praticamente concluída, já adicionei os comentários no arquivo 
`estruturaDB.txt` com o "script" para criação do banco e a explicação das
tabelas.

Comecei a rodar o banco, vou tentar rodar um PostgreSQL localmente, depois vejo
como colocar para rodar na web.

Aparentemente deu certo, para instalar o PostgreSQL utilizei o comando:

sudo apt update
sudo apt install postgresql postgresql-contrib

Para ver se estava rodando:

sudo systemctl status postgresql

E por fim, o PostgreSQL cria um usuário chamado `posgres`, alterei para ele,
fazendo com que possa adicionar as Queries diretamente no terminal:

sudo -u postgres psql

Sim, para issso, o Chat me orientou

Comecei o `init.sql`, que é o script a ser executado para iniciar o banco
A sintaxe é diferente da que eu vi no site, então estou adaptando.

OK, a principio finalizei o init.sql, fiz na mão e pedi para o Chat corrigir,
aparentemente tudo OK, vou tentar rodar e ver o resultado.

Para acessar o banco:
sudo -u postgres psql lojinha

Para executar o script SQL:
\i /caminho/init.sql

VALIDANDO TABELAS
Após rodar o script, vamos validar para saber se tudo foi criado.

- Listando tabelas:
lojinha=# \dt

- Inspecionando tabela:
lojinha=# \d nome_tabela 
(aperte q para sair)

Vou escrever alguns exemplos de inserção para testar a tabela, 
criei o insert_examples.sql para isso.

Vi que é possível cadastrar um item com valor negativo, já adicionei mais uma,
regra, registrei também como fazer o DROP na ordem correta.


## INTEGRAÇÃO BANCO E C#
Agora que o banco está testado e aprovado, vou começar a ver como utilizar o C#
para interagir com o mesmo.

Aparentemente, usaremos o Entity Framework Core (EF Core), que ainda não sei 
bem o que é.

Também trabalharemos com ORM, que é Object Relational Mapping, basicamente cri-
ando os objetos das tabelas em classes do C#.

INSTALANDO PACOTES
EF Core: dotnet add package Microsoft.EntityFrameworkCore
Provider PostgreSQL (o que diz como se comunicar com o PostgreSQL):
dotnet add package Microsoft.EntityFrameworkCore
Ferramentas do EF: dotnet add package Microsoft.EntityFrameworkCore.Design
(isso alterou o `lojinha.csproj`)

INICIANDO MODELS
O que é isso afinal? Nada mais que a representação das tabelas em C#.
Ficarão na pasta `Models`

Iniciando a classe Cliente.cs, que representará a tabela `clientes`

Agora chegou a hora de conectar o cliente com a tabela, usando DbContext.
Para isso que foi criada a pasta `Data`!

-> 15/05/2026

Comecei o AppDbContext, aqui está mais complicado. Entendi que essa é a interf-
ace de comunicação do programa com o Banco, onde vou dizer onde está o banco e
qual tabela cada classe representa, e de uma forma mágica ele vai linkar os do-
is e fazer as alterações no banco, sem que eu precise escrever queries SQL.

Teoricamente agora preciso criar a conexão real com o banco e testar, sim, o c-
hat está me guiando por esses vales estranhos.

Comecei a mexer com conexão com o banco, então adicionei o .gitignore para não 
subir o appsettings.Development.json, que terá os dados de acesso reais.

Estou alterando a senha do usuário `postgres` no banco de dados, utilizando:
ALTER USER postgres PASSWORD 'senha'; 
Obviamente com outra senha.

Adicionei um exemplo do appsettings

Precisei instalar o dotnet add package Npgsql.EntityFrameworkCore.PostgreSQL

Agora vou criar o primeiro endpoint para testar a conexão com o banco, que fará
a listagem dos clientes.

Tá, de alguma forma criei o enpoint /clientes, que teoricamente retorna o cont-
eúdo da tabela `clientes`, executando um `SELECT * FROM clientes`.

Publiquei e testei no Swagger, deu erro dizendo que a tabela `Clientes` não ex-
iste, ele deve ter tentado executar um `SELECT * FROM Clientes`. Terei que ref-
erenciar que `Clientes` representa a tabela `clientes`

Consegui corrigir o mapeamento da tabela, agora falta o mapeamento das colunas,
pois está tentando consultar a coluna `Id`, quando existe apenas a `id`. De al-
guma forma preciso fazer o mapeamento das colunas também, um de-para.

Começando a fazer o mapeamento dos campos...

Tá, consegui fazer o mapeamento, não foi tão difícil, fui fazendo um de cada v-
ez e testando no Swagger. O GET de clientes já funciona, agora vou para o POST.

Consegui criar o POST sozinho!! Já consigo adicionar no banco via API, só prec-
iso corrigir depois o tipo da data de nascimento, que está pedindo em UTC, qu-
ando poderia estar apenas `Date`.

Próximo passo é Excluir e alterar.

Feita a exclusão.

Feita a alteração do cliente.

Vou fazer agora a validação dos dados, para garantir que os dados não estejam 
vazios e que o e-mail e CPF sejam válidos.

Adicionada verificação de nome no Insert e Update.

-> 18/05/2026

Iniciando uma nova semana, tenho que finalizar até quarta, então o meu plano é
finalizar 100% do backend hoje e focar no Front e disponibilização web amanhã.

Para finalizar o back, preciso criar as outras Models e mapear tabelas e campos
, além de descobrir se já deixo o back disponível para depois colocar o front 
ou espero para colocar tudo junto. Enfim, mãos na massa!

Estou ficando incomodado com o tamanho e quantidade de comentários do Program.-
cs, mas vou deixar para limpar no final.

Agora vou focar em fazer as Models e mapeamentos!

Montei os Models e mapeei os campos, mas pelo que entendi, seria bom que o Ped-
ido tivesse um "atributo" Cliente, para dizer que o pedido é desse cliente, e 
vice-versa, o mesmo para os itens do pedido.

Estou criando os Endpoints! Agora está mais fácil, pois tenho os 4 modelos pro-
ntos, sendo necessário apenas adaptar.

Depois tenho que dar atenção às verificações, de, por exemplo, só criar um ped-
ido se o usuário informado existir, senão vai dar erro do banco. Também preciso
ver os Deletes, pois não consigo deletar um pedido que possua itens, sendo nec-
essário primeiro deletar os itens. Ver como fazer um Delete Cascade ou o que é
melhor nesse caso.

Adicionei a verificação para saber se o cliente existe antes de adicionar um p-
edido, vou partir para o CRUD dos ITENS. 

Depois vou ver para tirar o `set` dos IDs, pois o banco icrementa automaticame-
nte, não precisa (e nem pode) alterar na mão.

Também notei que o GET de itens e pedidos não tem filtro por pedido e cliente, 
sempre traz todos.

Também não estou pensando em paginação, se tiver 10000 itens ele vai consultar
e retornar todos de uma vez.

Estou padronizando os Returns de todos os endpoints agora.

Implementando também a consulta de um unico cliente/pedido/item.

Estou na etapa de colocar validações de quantidade, tamanho, formato de e-mail
e CPF, e acho que vou fazer direto nos endpoints. Imagino que o ideal seria cr-
iar uma função, por exemplo .validaCPF() e assim por diante para cada campo da
tabela, só não sei se vale a pena, porém ficaria muito mais organizado e sem r-
epetição de código.

Pedi umas dicas pro Chat, e vou separar em arquivos para facilitar a visualiza-
ção e manutenção, além de fazer as funções auxiliares.  

Separei os endpoints na pasta Endpoints, para deixar o Program.cs mais limpo.

Vou trabalhar nas validações agora, fazendo uma função para validar CPF, e-mail
e tal.

Percebi que estou sendo bem objetivo nesse documento, a ideia original era dar
mais ênfase à milha linha de raciocínio, mas de qualquer forma, espero que seja
útil! Nesse momento já está muito mais tranquilo de desenvolver, a sintaxe está
muito mais clara, e agora que boa parte está pronta, está sendo bem mais rápido
de simplesmente copiar e adaptar o que já está feito. 

Eu ia fazer os validadores retornarem True ou False, mas vou fazer retornarem 
uma lista de erros!

Mudança, vou fazer com que retorne um erro de cada vez.

Já fiz o grosso das validações, só não sei muito bem como vou colocar isso no 
PUT também, pois daria erro ao procurar pelo CPF e E-mail (falta validar e-m-
ail duplicado), mas após isso estaremos quase!

Adicionei um argumento opcional para informar o ID do cliente atual, que será 
enviado pelo PUT e não dará problema com o CPF e E-mail duplicados

(também deixei de colocar aqui os links pois estou acessando inúmeros)

TODO preciso adicionar a regra de maioridade
TODO blqueio alteração do pedido após 24h
TODO pedido com ao menos um item (isso vai incomodar)

Não havia considerado que um pedido precisa ter ao menos um item, estava deixa-
ndo livre para criar primeiro o pedido e depois adicionar os itens, precisarei
alterar os endpoints para que seja inserido os itens junto com o pedido e alte-
rado tudo junto também.

Está ficando meio caótico agora, essa alteração de modificar o pedido junto com
os itens deu uma quebrada legal, mas ao menos está funcionando!

Atualizando por aqui, comecei a pensar em como colocar o projeto no ar! Criei 
uma conta no render.com e criei um PostgreSQL lá usando o script de criação do
repositório, agora já não estou mais utilizando um banco de dados local, mas 
sim um banco em núvem! Assim é possível acessar de qualquer lugar.

Conectei com o dBeaver na minha máquina para monitorar, íbviamente o banco tem
muitas limitações de tamanho, mas apenas como um projeto temporário há de serv-
ir bem.

TAMO NO GAME!!!!
Publiquei o projeto no Render, agora ele fica vendo o repositório e atualizando
sozinho, de qualquer forma, já está disponível na web na url 
https://************.onrender.com/swagger/index.html hahahah

Obviamente minha API está totalmente aberta, então qualquer um pode encher meu
banco de dados e lotar de requisições, mas enfim, faz parte do processo, depois
a gente melhora.

Como o backend está publicado, estou mais tranquilo, pois achei que demoraria
muito, agora só falta fazer um front básico que consome as APIs e fazer os aju-
stes e enfeites necessários!

-> 19/05/2026

Praticamente o último dia do desafio, hoje tenho que ter um frontend básico pa-
ra interação do usuário.

Estou vendo certo o que vou utilizar, achei que o .NET possuia alguma ferramen-
ta de front, mas parece que não, penso em utilizar o clássico HTML + CSS + JS.

Vou ver ainda se ficará neste mesmo repositório ou se preciso criar outro, na
minha cabeça poderia ficar no mesmo.

Deixarei no mesmo repositório, dentro da pasta wwwroot, começando agora.

DOTNET WATCH descobri esse comando, que fica vendo mudanças, compilando e publ-
icando, sem que seja necessário dar DOTNET RUN toda vez. Muito bom.

Sim, estou utilizando IA para fazer o Front ;-;. Preferia tentar fazer na mão,
porém meu tempo é curto, e como o foco era aprender C#, precisei apelar.

